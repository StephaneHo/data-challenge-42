# Core data + ML
numpy>=2.0
pandas>=2.0
pillow>=10.0
tqdm>=4.65
matplotlib>=3.7
scikit-learn>=1.4

# Training (CPU build for local; on Colab use the CUDA build)
# For CPU local install:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
torch>=2.5
torchvision>=0.20

# Zero-shot pipeline (face parsing)
transformers>=4.40
safetensors>=0.4
huggingface_hub>=0.20

# Specific models for zero-shot
gdown>=6.1.0
insightface==1.0.1
opencv-python>=4.13.0.92
onnxruntime>=1.26.0
prettytable>=3.17.0
